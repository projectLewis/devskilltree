export interface Skill {
  level: number;
  name: string;
  name_uppercase: string;
  video: string;
  video_embed: string;
  resources: [string, string][];
  foundation: string[];
  track: string[];
}
